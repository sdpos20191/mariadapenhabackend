spring:
  application:
    name: ocorrencia-server
  cloud:
    config:
      fail-fast: true
      retry:
        initial-interval: 1000
        max-interval: 2000
        max-attempts: 100
      discovery:
        enabled: true
        service-id: config-server
      uri: http://config-server:9090
